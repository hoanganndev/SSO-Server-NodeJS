<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <title>SSO Socials</title>
        <script>
            $(document).ready(function () {
                const ssoToken = $("#ssoToken").val();
                const redireactURL = `${localStorage.getItem(
                    "clientURL"
                )}/code?ssoToken=${ssoToken}`;
                localStorage.removeItem("clientURL");
                window.location.href = redireactURL;
            });
        </script>
    </head>
    <body>
        <input id="ssoToken" hidden value="<%= ssoToken %>" />
    </body>
</html>
